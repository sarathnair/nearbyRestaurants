<div class="header">
    <h1>{{title}}</h1>
</div>

<div class='container'>
    <ms-content location={location}></ms-content>
</div>

{{#if ksfl}}
    {{location.latitude}}
    {{location.longitude}}
{{/if}}